Component({
  properties: {
    tabs: Array,
    clickCallback: Function
  },
  methods: {
    clickTab() {
      const foo = this.data.clickCallback
      if (foo && (typeof foo === 'function')) {
        foo()
      }
    }
  }
})
